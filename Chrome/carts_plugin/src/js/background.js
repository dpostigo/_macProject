

// var port = chrome.runtime.connect();
//  port.postMessage("hello message");
alert("hello");

var div = $('<div id="carts-dialog">heello</div>');
document.body.appendChild(div);

function cartsDialogDiv() {
	var div = $('<div id="carts-dialog">heello</div>');
	return div;
}

